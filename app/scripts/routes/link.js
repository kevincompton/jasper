/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var LinkRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return LinkRouter;
});
