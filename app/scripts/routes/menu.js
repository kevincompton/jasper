/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var MenuRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return MenuRouter;
});
